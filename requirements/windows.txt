# Windows-specific dependencies
# Desktop Agent Windows Requirements
# Includes base requirements + Windows media monitoring packages

-r base.txt

# Windows Media Monitoring (Windows SDK)
# Pinned to 1.0.0b10 which has pre-built wheels for Python 3.12
winsdk==1.0.0b10
